{
  "name": "productivity-assistant-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "benchmark": "node ./scripts/benchmark-ai.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.10",
    "wrangler": "^4.121.0"
  },
  "dependencies": {
    "jose": "^6.2.10"
  }
}
